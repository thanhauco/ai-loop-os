apiVersion: v1
kind: Service
metadata:
  name: ai-loop-os-api
  namespace: ai-loop-os
  labels:
    app.kubernetes.io/name: ai-loop-os
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-loop-os
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 4000
      targetPort: http
