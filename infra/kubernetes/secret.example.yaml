apiVersion: v1
kind: Secret
metadata:
  name: ai-loop-os-secrets
  namespace: ai-loop-os
type: Opaque
stringData:
  # Replace these values before applying to a real cluster.
  DATABASE_URL: ""
  OPERATOR_TOKEN: ""
  LLM_API_KEY: ""
